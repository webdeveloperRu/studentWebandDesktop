export default class Category {
  constructor(name, description, sort_postion, drip, status) {
    this.name = name;
    this.description = description;
    this.sort_postion = sort_postion;
    this.drip = drip;
    this.status = status;
  }
}